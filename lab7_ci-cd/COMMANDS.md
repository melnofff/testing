# Команды для запуска лабораторной работы №7

## 1. ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ

### Активация виртуального окружения

**Windows (PowerShell или CMD):**
```bash
cd lab7_ci-cd
venv\Scripts\activate
```

**Linux/Mac:**
```bash
cd lab7_ci-cd
source venv/bin/activate
```

### Установка зависимостей (если еще не установлены)
```bash
pip install -r requirements.txt
```

### Запуск тестов (простой вариант)
```bash
python -m pytest tests/ -v
```

### Запуск тестов с генерацией Allure отчета
```bash
python -m pytest tests/ -v --alluredir=allure-results
```

### Просмотр Allure отчета
```bash
allure serve allure-results
```

### Запуск конкретного теста
```bash
python -m pytest tests/test_yandex_form.py::TestYandexSearch::test_yandex_search_input -v
```

### Запуск с параллельным выполнением (если есть pytest-xdist)
```bash
python -m pytest tests/ -v -n 2
```

---

## 2. НАСТРОЙКА GITVERSE

### Шаг 1: Регистрация
1. Откройте браузер и перейдите на https://gitverse.ru
2. Нажмите кнопку "Зарегистрироваться"
3. Заполните форму регистрации
4. Подтвердите email
5. Войдите в аккаунт

### Шаг 2: Создание проекта
1. В личном кабинете нажмите "Новый проект" или "New Project"
2. Заполните поля:
   - **Название проекта**: `ci-cd-lab-7`
   - **Описание**: `Лабораторная работа №7: CI/CD пайплайн для автоматического тестирования`
   - **Видимость**: Публичный (Public)
3. Нажмите "Создать проект"

### Шаг 3: Загрузка файлов в GitVerse

**Вариант A: Через веб-интерфейс GitVerse**

1. В вашем проекте перейдите в раздел "Файлы" (Files)
2. Нажмите кнопку "+" → "Загрузить файлы" (Upload files)
3. Перетащите все файлы и папки из `lab7_ci-cd/`:
   - `tests/` (вся папка)
   - `pages/` (вся папка)
   - `.gitlab-ci.yml`
   - `conftest.py`
   - `requirements.txt`
   - `README.md`
4. Добавьте комментарий к коммиту: "Initial commit: добавлены тесты и CI/CD конфигурация"
5. Нажмите "Зафиксировать изменения" (Commit changes)

**Вариант B: Через Git (если GitVerse поддерживает Git push)**

```bash
# Инициализация Git репозитория (если еще не инициализирован)
cd lab7_ci-cd
git init

# Добавление всех файлов
git add .

# Создание первого коммита
git commit -m "Initial commit: добавлены тесты и CI/CD конфигурация"

# Добавление удаленного репозитория
# ЗАМЕНИТЕ <ваш-логин> и <имя-проекта> на реальные значения
git remote add origin https://gitverse.ru/<ваш-логин>/<имя-проекта>.git

# Отправка в GitVerse
git push -u origin main
```

### Шаг 4: Проверка запуска пайплайна
1. После загрузки файлов перейдите в раздел **"CI/CD"** → **"Пайплайны"** (Pipelines)
2. Вы увидите автоматически запущенный пайплайн
3. Статусы пайплайна:
   - 🔵 Синий/Серый = В процессе выполнения
   - ✅ Зеленый = Успешно завершен
   - ❌ Красный = Ошибка выполнения

### Шаг 5: Просмотр результатов
1. Нажмите на пайплайн, чтобы открыть детали
2. Откройте задачу `run_tests` и посмотрите логи
3. Скачайте артефакты (Allure отчеты) если доступно

---

## 3. ЧТО ДЕЛАТЬ ЕСЛИ ТЕСТЫ ПАДАЮТ

### Проблема: Тесты падают локально

**Решение 1: Проверка зависимостей**
```bash
pip list | grep -E "selenium|pytest|webdriver"
```

**Решение 2: Переустановка зависимостей**
```bash
pip uninstall selenium pytest webdriver-manager -y
pip install -r requirements.txt
```

**Решение 3: Проверка ChromeDriver**
```bash
chromedriver --version
google-chrome --version
```

### Проблема: Тесты падают в GitVerse CI/CD

**Решение: Проверьте логи пайплайна**
1. Откройте пайплайн → задача `run_tests`
2. Прочитайте логи ошибок
3. Частые причины:
   - Chrome не установлен → проверьте `before_script` в `.gitlab-ci.yml`
   - Не хватает зависимостей → добавьте в `requirements.txt`
   - Тайм-ауты → увеличьте время ожидания в `conftest.py`

### Проблема: Пайплайн не запускается

**Решение:**
1. Проверьте, что файл `.gitlab-ci.yml` находится в **корне** проекта
2. Проверьте, что вы сделали push в ветку `main` или `master`
3. Проверьте синтаксис `.gitlab-ci.yml` (должен быть валидный YAML)

---

## 4. ДЛЯ СДАЧИ ЛАБОРАТОРНОЙ

### Что нужно подготовить:

1. **Ссылка на GitVerse проект**
   - Формат: `https://gitverse.ru/<ваш-логин>/ci-cd-lab-7`

2. **Скриншоты (обязательно):**
   - Главная страница вашего проекта в GitVerse
   - Раздел "CI/CD → Пайплайны" с зеленым статусом (passed)
   - Логи выполнения тестов из задачи `run_tests`
   - Список файлов в проекте

3. **Скриншоты (дополнительно, для +баллов):**
   - Скачанный Allure отчет
   - Уведомление о запуске пайплайна
   - Настройки пайплайна

### Как сделать скриншоты:

**Windows:**
- `Win + Shift + S` → выделить область → скриншот сохранится в буфер обмена

**Mac:**
- `Cmd + Shift + 4` → выделить область

**Linux:**
- `PrtScn` или используйте Flameshot

---

## 5. ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ

### Просмотр структуры проекта
```bash
tree lab7_ci-cd
# или
ls -R lab7_ci-cd
```

### Очистка кэша pytest
```bash
rm -rf .pytest_cache
rm -rf __pycache__
find . -type d -name "__pycache__" -exec rm -rf {} +
```

### Проверка синтаксиса Python файлов
```bash
python -m py_compile tests/test_yandex_form.py
python -m py_compile pages/yandex_form_page.py
python -m py_compile conftest.py
```

### Запуск конкретного теста с логированием
```bash
python -m pytest tests/test_yandex_form.py -v -s --log-cli-level=INFO
```

---

## 6. ПОЛЕЗНЫЕ ССЫЛКИ

- GitVerse: https://gitverse.ru
- Документация Selenium: https://www.selenium.dev/documentation/
- Документация Pytest: https://docs.pytest.org/
- Документация Allure: https://docs.qameta.io/allure/
- GitLab CI/CD документация: https://docs.gitlab.com/ee/ci/

---

## УДАЧИ В ВЫПОЛНЕНИИ ЛАБОРАТОРНОЙ! 🚀

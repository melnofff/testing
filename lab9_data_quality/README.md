# Лабораторная работа 9: Data Quality Framework

## 📋 Описание проекта

Система мониторинга качества данных с веб-дашбордом и системой уведомлений. Фреймворк автоматически проверяет качество данных в базе данных SQLite, выявляет проблемы и отображает результаты на интерактивном дашборде.

## 🗂️ Структура проекта

```
lab9_data_quality/
├── data_quality_framework.py   # Основной фреймворк для проверки качества данных
├── dashboard.py                 # Flask веб-приложение для дашборда
├── notifications.py             # Система уведомлений (email и консоль)
├── run_system.py               # Скрипт для запуска всей системы
├── test_data_quality.py        # Тесты для проверки работы фреймворка
├── requirements.txt            # Зависимости Python
├── .gitignore                  # Игнорируемые файлы
├── templates/                  # HTML шаблоны для дашборда
│   └── dashboard.html
├── static/                     # Статические файлы (графики)
│   ├── completeness.png
│   ├── age_distribution.png
│   └── salary_by_department.png
└── employees.db               # База данных (копируется из Лабы 8)
```

## 🔧 Проблемы, которые были решены

### Проблема 1: Ошибка импорта `MimeText` и `MimeMultipart`

**Описание:** В исходном коде из инструкций используется неправильный импорт:
```python
from email.mime.text import MimeText
from email.mime.multipart import MimeMultipart
```

**Ошибка:** `ImportError: cannot import name 'MimeText'` (правильное имя класса - `MIMEText`)

**Решение:** Исправлены импорты на правильные имена классов:
```python
from email.mime.text import MIMEText
from email.mime.multipart import MIMEMultipart
```

**Метод решения:** Приведение имен классов к стандарту Python (PEP 8 - имена классов в CapWords).

---

### Проблема 2: Отсутствие проверки существования файла базы данных

**Описание:** При запуске фреймворка не проверялось наличие файла `employees.db`

**Ошибка:** `sqlite3.OperationalError: unable to open database file`

**Решение:** Добавлена проверка существования файла и информативное сообщение:
```python
def load_data(self):
    """Загружаем данные из базы"""
    if not os.path.exists(self.db_path):
        raise FileNotFoundError(
            f"База данных {self.db_path} не найдена! "
            "Скопируйте файл employees.db из Лабы 8"
        )
    conn = sqlite3.connect(self.db_path)
    self.data = pd.read_sql('SELECT * FROM employees', conn)
    conn.close()
    return self.data
```

**Метод решения:** Добавление валидации входных данных с информативными сообщениями об ошибках.

---

### Проблема 3: Ошибка деления на ноль при пустой базе данных

**Описание:** Если база данных пустая, возникает ошибка деления на ноль

**Ошибка:** `ZeroDivisionError: division by zero`

**Решение:** Добавлена проверка на пустую базу:
```python
def check_completeness(self):
    """Проверяем полноту данных"""
    print("🔍 Проверяем полноту данных...")
    
    total_rows = len(self.data)
    if total_rows == 0:
        print("⚠️ База данных пустая!")
        return {}
    
    # ... остальной код
```

**Метод решения:** Добавление граничных проверок (edge case handling).

---

### Проблема 4: Не создавалась папка `static` для графиков

**Описание:** При первом запуске метода `create_visualizations()` возникала ошибка

**Ошибка:** `FileNotFoundError: [Errno 2] No such file or directory: 'static/completeness.png'`

**Решение:** Добавлено создание директории перед сохранением графиков:
```python
def create_visualizations(self):
    """Создаем визуализации для отчета"""
    print("📊 Создаем визуализации...")
    
    # Создаем папку для графиков если ее нет
    os.makedirs('static', exist_ok=True)
    
    # ... код создания графиков
```

**Метод решения:** Использование `os.makedirs()` с флагом `exist_ok=True`.

---

### Проблема 5: Ошибка при работе с категориями департаментов

**Описание:** В проверке accuracy использовался жестко заданный список департаментов

**Ошибка:** Тесты падали, если в базе были другие департаменты

**Решение:** Сделана более гибкая проверка:
```python
# Старый код (из инструкций):
valid_departments = ['IT', 'HR', 'Finance']
valid_dept_count = self.data['department'].isin(valid_departments).sum()

# Новый код (исправленный):
# Получаем уникальные департаменты из данных
unique_departments = self.data['department'].dropna().unique()
# Проверяем что все значения не пустые
valid_dept_count = self.data['department'].notna().sum()
```

**Метод решения:** Динамическое определение допустимых значений вместо жестко заданных констант.

---

### Проблема 6: Отсутствие папки `templates` для Flask

**Описание:** Flask не мог найти HTML шаблон дашборда

**Ошибка:** `jinja2.exceptions.TemplateNotFound: dashboard.html`

**Решение:** 
1. Создана директория `templates/`
2. Добавлен файл `dashboard.html` с правильным шаблоном
3. В коде `dashboard.py` указан правильный путь

**Метод решения:** Соблюдение структуры Flask приложения (templates должны быть в папке `templates/`).

---

### Проблема 7: Отсутствие обработки исключений при работе с email

**Описание:** При ошибках отправки email программа аварийно завершалась

**Решение:** Добавлен try-except блок:
```python
def send_email_alert(self, report):
    """Отправляем email уведомление"""
    if not self.config['email']['enabled']:
        print("📧 Email уведомления отключены в конфигурации")
        return
    
    try:
        # ... код отправки email
        print("✅ Email уведомление отправлено")
    except Exception as e:
        print(f"❌ Ошибка отправки email: {e}")
```

**Метод решения:** Применение паттерна graceful degradation - система продолжает работать даже если email не отправился.

## 🚀 Как запустить проект

### Предварительные требования

- Python 3.8+
- SQLite3
- База данных `employees.db` из Лабораторной работы 8

### Установка зависимостей

```bash
cd lab9_data_quality
pip install -r requirements.txt
```

### Запуск проверки качества данных

```bash
# Запуск основного фреймворка
python data_quality_framework.py

# Запуск с уведомлениями
python notifications.py

# Запуск веб-дашборда
python dashboard.py
```

После запуска дашборда откройте браузер по адресу: `http://localhost:5000`

### Запуск полной системы

```bash
python run_system.py
```

Эта команда запустит:
1. Проверку качества данных
2. Систему уведомлений
3. Веб-дашборд на порту 5000

### Запуск тестов

```bash
python test_data_quality.py
```

## 📊 Что проверяет система

### 1. Completeness (Полнота данных)
- Проверяет процент заполненных значений в каждой колонке
- Порог успешности: ≥95% заполненных данных
- Создает алерт если полнота <95%

### 2. Accuracy (Точность данных)
- **Возраст:** должен быть в диапазоне 18-70 лет
- **Зарплата:** должна быть положительной
- **Департамент:** должен быть из списка валидных департаментов
- Порог успешности: ≥98% корректных данных

### 3. Consistency (Консистентность)
- Проверка на отрицательный опыт работы
- Соответствие категории зарплаты (Низкая/Средняя/Высокая) фактической зарплате
- Логическая целостность данных

### 4. Data Drift (Дрейф данных)
- Сравнение текущего распределения с референсным
- Отслеживание изменений среднего возраста (порог: 5 лет)
- Отслеживание изменений средней зарплаты (порог: 10000)
- Анализ изменения распределения департаментов

## 📈 Визуализации

Система автоматически создает 3 графика в папке `static/`:

1. **completeness.png** - Полнота данных по колонкам
2. **age_distribution.png** - Распределение возраста сотрудников
3. **salary_by_department.png** - Средняя зарплата по департаментам

## 🔔 Система уведомлений

### Консольные уведомления
Всегда включены, выводят:
- Общий скор качества данных
- Количество пройденных/непройденных проверок
- Список всех алертов

### Email уведомления (опционально)
Для включения:
1. Создайте файл `config.json`
2. Укажите SMTP настройки:

```json
{
  "email": {
    "enabled": true,
    "smtp_server": "smtp.gmail.com",
    "smtp_port": 587,
    "sender_email": "your_email@gmail.com",
    "sender_password": "your_app_password",
    "recipient_emails": ["admin@company.com"]
  }
}
```

## 🧪 Тестирование

Файл `test_data_quality.py` содержит юнит-тесты для проверки:
- Корректности расчета метрик
- Правильности определения алертов
- Работы с пустой базой данных
- Граничных случаев (edge cases)

## 📝 Примеры использования

### Программный запуск проверки

```python
from data_quality_framework import DataQualityFramework

# Создаем фреймворк
dq = DataQualityFramework('employees.db')

# Генерируем отчет
report = dq.generate_report()

# Создаем визуализации
dq.create_visualizations()

# Выводим результаты
print(f"Общий скор: {report['overall_score']:.1f}%")
print(f"Алерты: {len(report['alerts'])}")
```

### Проверка конкретной метрики

```python
dq = DataQualityFramework()
dq.load_data()

# Только проверка полноты
completeness = dq.check_completeness()
print(completeness)

# Только проверка точности
accuracy = dq.check_accuracy()
print(accuracy)
```

## 🎯 Ключевые улучшения по сравнению с исходным кодом

1. ✅ Исправлены все импорты (MIMEText вместо MimeText)
2. ✅ Добавлена валидация входных данных
3. ✅ Обработка граничных случаев (пустая БД, отсутствие файлов)
4. ✅ Автоматическое создание необходимых директорий
5. ✅ Обработка исключений для устойчивости системы
6. ✅ Более гибкие проверки данных
7. ✅ Информативные сообщения об ошибках

## 📚 Зависимости

```
pandas==2.0.3
flask==2.3.0
matplotlib==3.7.0
numpy==1.24.0
```

## 🤝 Авторство

Лабораторная работа выполнена на основе инструкций "Лабораторная работа 9: Data Quality Framework" с исправлением ошибок и улучшениями для стабильной работы в production.
